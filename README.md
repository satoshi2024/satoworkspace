お疲れ様です。〇〇です。
QA #[チケット番号]（[機能名]における[対象伝票]が重複作成される不具合）について、原因と修正方針が特定できましたのでご報告いたします。
結論から申し上げますと、バックエンドの判定ロジックのミスではなく、フロント画面（[画面ファイル名]）から「前回値」のデータが送信されていないことが根本原因でした。
■ 発生メカニズム（原因詳細）
 * 画面側のHidden項目漏れ：[画面ファイル名] にて、[前回値項目] が画面表示用としてのみ実装されており、フォーム送信用の <input type="hidden"> が存在していませんでした。
 * バックエンドでの誤判定：更新ボタン押下時、サーバー側は前回値を未送信（null）として受け取り、金額 0 に変換します。
 * 不要な伝票作成：差額判定処理（[処理メソッド名]）において、実際の今回値（例：〇〇円）と欠落した前回値（0円）を比較してしまうため、「増額があった」と誤認し、不要な[対象伝票]を作成していました。
 * ※QA検証で「再現しない」ケースがあったのは、そのテスト対象データの本来の「前回値」がもともと 0 だったため、比較結果が一致（0 = 0）し、バグが表面化しなかったためです。
■ 修正方針（対応内容）
[画面ファイル名] を修正します。
[区分A]（[項目名A]）および [区分B]（[項目名B]）の各対象箇所に、それぞれ以下の形式でHiddenタグを追加し、正しい前回値をサーバー側へ引き渡すように対応します。
<!-- 修正イメージ -->
<input type="hidden" name="[パラメータ名]" value="${[フォームオブジェクト名].[パラメータ名]}">

上記の方針で画面側の修正およびローカルでの動作検証を進めてよろしいでしょうか。
ご確認のほど、よろしくお願いいたします。
